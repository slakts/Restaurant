@page
@model LoginModel

@{
    ViewData["Title"] = "Log in";
    Layout = null;
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Giriş Yap</title>
    <link href="https://cdn.jsdelivr.net/npm/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/login.css">
</head>
<body>
    <div class="login-container">
        <h2><i class='bx bxs-user'></i> Giriş Yap</h2>
        <form id="login-form" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="input-group">
                <i class='bx bxs-envelope'></i>
                <input asp-for="Input.Email" class="form-control" placeholder="E-posta adresiniz" required>
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <div class="input-group">
                <i class='bx bxs-lock-alt'></i>
                <input asp-for="Input.Password" type="password" class="form-control" placeholder="Şifreniz" required>
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
            <div class="form-group">
                <div class="checkbox">
                    <label asp-for="Input.RememberMe">
                        <input asp-for="Input.RememberMe"> Beni Hatırla
                    </label>
                </div>
            </div>
            <button type="submit" class="btn-login"><i class='bx bx-log-in'></i> Giriş Yap</button>
            <p class="error-message" id="error-message"></p>
        </form>
        <p class="forgot-password"><a asp-page="./ForgotPassword">Şifrenizi mi unuttunuz?</a></p>
    </div>
    <script src="~/js/login.js"></script>
</body>
</html>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}